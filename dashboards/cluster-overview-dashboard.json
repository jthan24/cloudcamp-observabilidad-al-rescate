{
  "annotations": {
    "list": []
  },
  "panels": [
    {
      "type": "graph",
      "title": "CPU Usage (cluster)",
      "targets": [
        {
          "expr": "sum(rate(container_cpu_usage_seconds_total[5m])) by (instance)"
        }
      ]
    },
    {
      "type": "graph",
      "title": "Memory Usage (cluster)",
      "targets": [
        {
          "expr": "sum(container_memory_usage_bytes) by (instance)"
        }
      ]
    }
  ],
  "title": "Cluster Overview (Demo)"
}